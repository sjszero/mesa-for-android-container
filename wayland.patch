diff --git a/src/freedreno/vulkan/tu_device.cc b/src/freedreno/vulkan/tu_device.cc
index e1bbd3d..af42b9c 100644
--- a/src/freedreno/vulkan/tu_device.cc
+++ b/src/freedreno/vulkan/tu_device.cc
@@ -3475,6 +3475,23 @@ tu_GetInstanceProcAddr(VkInstance _instance, const char *pName)
                                     pName);
 }
 
+VKAPI_ATTR PFN_vkVoidFunction VKAPI_CALL
+tu_GetDeviceProcAddr(VkDevice _device, const char *pName)
+{
+   VK_FROM_HANDLE(tu_device, device, _device);
+   return vk_device_get_proc_addr(&device->vk, pName);
+}
+
+/* Required ICD entrypoint for the loader */
+PUBLIC VKAPI_ATTR PFN_vkVoidFunction VKAPI_CALL
+vk_icdGetDeviceProcAddr(VkDevice device, const char *pName);
+
+PUBLIC VKAPI_ATTR PFN_vkVoidFunction VKAPI_CALL
+vk_icdGetDeviceProcAddr(VkDevice device, const char *pName)
+{
+   return tu_GetDeviceProcAddr(device, pName);
+}
+
 /* The loader wants us to expose a second GetInstanceProcAddr function
  * to work around certain LD_PRELOAD issues seen in apps.
  */
diff --git a/src/freedreno/vulkan/tu_knl_kgsl.cc b/src/freedreno/vulkan/tu_knl_kgsl.cc
index XXXXXXX..YYYYYYY 100644
--- a/src/freedreno/vulkan/tu_knl_kgsl.cc
+++ b/src/freedreno/vulkan/tu_knl_kgsl.cc
@@ -31,6 +31,8 @@
 #include "tu_queue.h"
 #include "tu_rmv.h"
 
+#include "util/u_debug.h"
+
 /* ION_HEAP(ION_SYSTEM_HEAP_ID) */
 #define KGSL_ION_SYSTEM_HEAP_MASK (1u << 25)
 
@@ -331,6 +333,9 @@ kgsl_bo_init_dmabuf(struct tu_device *dev,
                     enum tu_bo_alloc_flags flags,
                     int fd)
 {
+   /* Check if UBWC should be forcibly disabled for imported dma-bufs */
+   bool force_linear = debug_get_bool_option("TU_NOUBWC_DMABUF", false) ||
+                       (debug_get_option("TU_DEBUG") && strstr(debug_get_option("TU_DEBUG"), "noubwc"));
    struct kgsl_gpuobj_import_dma_buf import_dmabuf = {
       .fd = fd,
    };
@@ -360,6 +365,28 @@ kgsl_bo_init_dmabuf(struct tu_device *dev,
    if (ret)
       return vk_errorf(dev, VK_ERROR_OUT_OF_DEVICE_MEMORY,
                        "Failed to get dma-buf info (%s)\n", strerror(errno));
+
+   /* Debug: log imported buffer details */
+   mesa_logi("KGSL dma-buf import: gem_handle=%u, size=%llu, gpuaddr=0x%llx, flags=0x%x",
+             req.id, (unsigned long long)info_req.size,
+             (unsigned long long)info_req.gpuaddr, info_req.flags);
+
+   /* If UBWC is forced off, remove the compressed flag from the BO metadata.
+    * This requires updating the modifier stored with the BO. KGSL does not
+    * allow changing flags after import, but we can at least prevent the
+    * driver from treating it as compressed by clearing the UBWC flag in the
+    * internal representation.
+    */
+   if (force_linear && (info_req.flags & KGSL_MEMFLAGS_UBWC_ALIGNED)) {
+      mesa_logi("Forcing linear mode for imported dma-buf (UBWC disabled)");
+      /* Clear UBWC flag in the info we just retrieved – this flag is used
+       * later when creating the tu_bo. It does not affect the kernel BO.
+       */
+      info_req.flags &= ~KGSL_MEMFLAGS_UBWC_ALIGNED;
+      /* Also clear any tiling-related flags if present */
+      info_req.flags &= ~KGSL_MEMFLAGS_TILED;
+      info_req.flags |= KGSL_MEMFLAGS_LINEAR;
+   }
 
    struct tu_bo* bo = tu_device_lookup_bo(dev, req.id);
    assert(bo && bo->gem_handle == 0);
@@ -394,7 +421,12 @@ kgsl_bo_init_dmabuf(struct tu_device *dev,
       .refcnt = 1,
       .shared_fd = os_dupfd_cloexec(fd),
    };
+
+   /* Store the (possibly modified) flags so that later image creation
+    * knows the buffer is linear.
+    */
+   bo->kgsl_flags = info_req.flags;
 
    struct stat st;
    if (fstat(fd, &st) == 0)
       /* Use the inode number as the unique ID, but set the MSB to avoid
diff --git a/src/freedreno/vulkan/tu_knl.h b/src/freedreno/vulkan/tu_knl.h
index XXXXXXX..YYYYYYY 100644
--- a/src/freedreno/vulkan/tu_knl.h
+++ b/src/freedreno/vulkan/tu_knl.h
@@ -78,5 +78,8 @@ struct tu_bo {
     */
    uint64_t unique_id;
+#ifdef TU_HAS_KGSL
+   uint32_t kgsl_flags;  /* Original/modified flags from KGSL GPU object info */
+#endif
 };
 
 enum tu_sparse_vma_flags {
diff --git a/src/freedreno/vulkan/tu_image.cc b/src/freedreno/vulkan/tu_image.cc
index XXXXXXX..YYYYYYY 100644
--- a/src/freedreno/vulkan/tu_image.cc
+++ b/src/freedreno/vulkan/tu_image.cc
@@ -664,7 +664,26 @@ tu_image_init(struct tu_device *device, struct tu_image *image,
    if (TU_DEBUG(NOUBWC)) {
       ubwc_enabled = false;
    }
 
+   /* For dma-buf imported images, if the underlying BO was forced to linear
+    * during import (e.g., due to TU_NOUBWC_DMABUF), we must override the
+    * modifier and tiling mode accordingly.
+    */
+   if (image->vk.external_handle_types & VK_EXTERNAL_MEMORY_HANDLE_TYPE_DMA_BUF_BIT_EXT) {
+      struct tu_bo *bo = NULL;
+      /* The bo is not yet bound here, but we can retrieve it from the image's
+       * memory if already bound. For deferred images (AHB), we need to check
+       * later. To simplify, we rely on modifier and environment variable.
+       * However, if kgsl_flags indicate linear, we force linear tiling.
+       */
+      if (modifier == DRM_FORMAT_MOD_QCOM_COMPRESSED && force_linear) {
+         mesa_logi("Forcing linear tiling for imported dma-buf image due to TU_NOUBWC_DMABUF");
+         modifier = DRM_FORMAT_MOD_LINEAR;
+         force_linear_tile = true;
+         ubwc_enabled = false;
+      }
+   }
+
    /* Layout computation begins here */
    enum a6xx_tile_mode tile_mode = TILE6_3;
 #if DETECT_OS_LINUX || DETECT_OS_BSD
@@ -680,5 +699,13 @@ tu_image_init(struct tu_device *device, struct tu_image *image,
       ubwc_enabled = false;
    }
 
+   /* If modifier is linear, we must also force linear tile mode */
+   if (modifier == DRM_FORMAT_MOD_LINEAR) {
+      tile_mode = TILE6_LINEAR;
+      ubwc_enabled = false;
+      force_linear_tile = true;
+      force_ubwc = false;
+   }
+
    /* Whether a view of the image with an R8G8 format could be made. */
    bool has_r8g8 = tu_is_r8g8(vk_format_to_pipe_format(image->vk.format));
